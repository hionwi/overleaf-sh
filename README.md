# Overleaf to GitHub Sync Tools

本仓库包含两个核心脚本，旨在帮助使用 `overleaf-toolkit` 部署的自建 Overleaf (Community Edition) 实现自动化管理与 GitHub 同步。

* **`overleaf.sh`**: 用于快速管理 Overleaf 容器的中文部署。
* **`git-overleaf-sync.sh`**: 用于将 Overleaf 容器内的项目自动同步到 GitHub 私有仓库。

---

## 🚀 脚本：git-overleaf-sync.sh 使用指南

由于同步过程涉及容器内外的文件交互及 GitHub 身份验证，请按照以下步骤配置环境。

### 1. 配置 GitHub SSH 访问权限

脚本需要容器具有向 GitHub 推送代码的权限。请依次执行以下操作：

#### 进入 Overleaf 容器

```bash
docker exec -it sharelatex bash

```

#### 生成 SSH 密钥

在提示输入 `passphrase`（密码短语）时，**直接按回车留空**，以便脚本自动化运行。

```bash
mkdir -p /root/.ssh
ssh-keygen -t ed25519 -C "overleaf-docker-key" -f /root/.ssh/id_ed25519

```

#### 将公钥添加到 GitHub

1. 运行查看命令并复制输出内容：
```bash
cat /root/.ssh/id_ed25519.pub

```


2. 前往 [GitHub SSH Settings](https://github.com/settings/keys)。
3. 点击 **New SSH key**，Title 填 `Overleaf-Docker`，粘贴公钥并保存。

#### 验证连接

在容器内运行，确保能够成功通过身份校验：

```bash
ssh -T git@github.com
# 提示 "Are you sure..." 时，输入 yes

```

看到 `Hi [Username]! You've successfully authenticated` 后，输入 `exit` 退出容器。

---

### 2. 使用 git-overleaf-sync.sh 进行同步

确保你已经赋予脚本执行权限：

```bash
chmod +x git-overleaf-sync.sh
```

在overleaf中创建新项目

执行脚本

#### 脚本逻辑简介：

1. **导出**：脚本会触发 Overleaf 容器内的项目导出到本地仓库。
2. **同步**：利用 Git 将本地导出的项目内容推送到远程 GitHub 仓库。

---

## 📝 注意事项

* **安全性**: 请妥善保管生成的私钥，不要将其提交到公共代码库。
* **自动化**: 建议将 `git-overleaf-sync.sh` 加入 `crontab` 定时任务，实现项目的每日自动备份。

